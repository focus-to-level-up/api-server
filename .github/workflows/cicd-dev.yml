name: CI/CD for Development (Docker Compose)

# Disabled: Using ECS deployment (cicd-ecs-dev.yml)
# on:
#   push:
#     branches: [ main ]

on:
  workflow_dispatch:  # Manual trigger only

permissions:
  contents: read

jobs:
  call-cicd-template:
    name: Call CI/CD Template for Development
    uses: ./.github/workflows/cicd-template.yml
    secrets: inherit
    with:
      environment: 'dev'
