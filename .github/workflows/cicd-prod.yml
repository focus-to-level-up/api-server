name: CI/CD for Production (Docker Compose)

# Disabled: Using ECS deployment (cicd-ecs-prod.yml)
# on:
#   push:
#     tags:
#       - '*'

on:
  workflow_dispatch:  # Manual trigger only

permissions:
  contents: read

jobs:
  call-cicd-template:
    name: Call CI/CD Template for Production
    uses: ./.github/workflows/cicd-template.yml
    secrets: inherit
    with:
      environment: 'prod'
