# application-local.yml (로컬 개발용)
spring:
  # Data db (API용)
  datasource-data:
    jdbc-url: jdbc:mysql://localhost:13307/focus_db?useSSL=false&useUnicode=true&serverTimezone=UTC&characterEncoding=UTF-8
    username: focus
    password: focus
    driver-class-name: com.mysql.cj.jdbc.Driver

  # Meta DB (Batch 메타데이터용)
  datasource-meta:
    jdbc-url: jdbc:mysql://localhost:13307/focus_meta_db?useSSL=false&useUnicode=true&serverTimezone=UTC&characterEncoding=UTF-8
    username: focus
    password: focus
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
<<<<<<< HEAD
#    hibernate:
#      naming:
#        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
#        physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
    show-sql: true
    open-in-view: false
    generate-ddl: true
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        format_sql: true
#        naming:
#          # 4. 이것이 카멜케이스 -> 스네이크케이스 변환 설정입니다. (하이픈(-) 사용)
#          physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
#          implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl

  batch:
    jdbc:
      schema: classpath:org/springframework/batch/core/schema-mysql.sql
      initialize-schema: embedded
=======
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
>>>>>>> origin

  sql:
    init:
      mode: always
      data-locations: ${MASTER_DATA_PATH}
      encoding: UTF-8
      continue-on-error: false

# Hibernate SQL 로깅 (파라미터 값 포함)
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.hibernate.orm.jdbc.bind: TRACE
