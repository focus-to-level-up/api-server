bootJar { enabled = false } // 실행 파일 아님
jar { enabled = true }

dependencies {
    // .env
    api 'io.github.cdimascio:dotenv-java:3.0.0'
    // json
    implementation 'com.fasterxml.jackson.core:jackson-databind'
}
