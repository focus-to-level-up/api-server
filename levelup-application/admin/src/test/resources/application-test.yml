spring:
  datasource-data:
    jdbc-url: jdbc:h2:mem:admin-test;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    open-in-view: false
    properties:
      hibernate:
        hbm2ddl:
          auto: create

admin:
  initial:
    username: initial-admin
    password: strong-password
